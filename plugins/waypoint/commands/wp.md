---
description: "Alias for /waypoint — checkpoint this session's state to a waypoint doc and produce a handoff prompt."
---

Invoke the `waypoint` skill now, exactly as if the user had typed `/waypoint $ARGUMENTS`. Load
that skill's instructions (`${CLAUDE_PLUGIN_ROOT}/skills/waypoint/SKILL.md`) and follow them
in full — this command carries no behavior of its own beyond forwarding to it.
