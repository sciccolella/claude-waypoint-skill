---
description: "Alias for /waypoint-continue — auto-detect and resume from the right waypoint doc."
---

Invoke the `waypoint-continue` skill now, exactly as if the user had typed
`/waypoint-continue $ARGUMENTS`. Load that skill's instructions
(`${CLAUDE_PLUGIN_ROOT}/skills/waypoint-continue/SKILL.md`) and follow them in full — this
command carries no behavior of its own beyond forwarding to it.
